# Domain Docs

Single-context repo. One `CONTEXT.md` + `docs/adr/` at the repo root, created lazily by `/domain-modeling` when terms or decisions get resolved.

If any of these files don't exist, proceed silently. Do not flag their absence.