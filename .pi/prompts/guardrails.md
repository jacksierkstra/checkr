---
description: Build (or review) repo-specific reuse guardrails
argument-hint: [init|review]
---
Read `docs/agents/commands/guardrails.md` and execute it as the current task. If you were invoked with a mode argument ($1), pass it through; otherwise resolve it per the doc (init if no `guardrails.config.*` exists, else review).
